#include "main.h"

/**
*_strlen-returns the length of a string
*@s:string
*Return: length as in integer
*/

int _strlen(char *s)
{
	char *s[];
	int length;

	length = strlen(*s);

	return (length);
}
